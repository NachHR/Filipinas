{
  "name": "filipinas-travel-pwa",
  "version": "8.10.0",
  "private": true,
  "description": "Static travel PWA; development-only regression tests, no runtime dependencies.",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "devDependencies": {
    "jsdom": "30.0.1"
  }
}
